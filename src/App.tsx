import {
  BrowserRouter,
  Routes,
  Route,
  Link
} from "react-router-dom"

function App() {
  <BrowserRouter>
  <Routes>
    <Route path="/" element={}/>
    <Route path="*" element={}/>
  </Routes>
  </BrowserRouter>
}

export default App
